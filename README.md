My Doom Emacs config files. Some plugins for web development and a nice theme. To use, install Doom Emacs from instructions, then replace the files in ~/.doom.d with these ones. Run ~/.emacs.d/bin/doom sync. 
